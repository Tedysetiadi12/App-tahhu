<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="#11D1722E" />

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="@color/accentColor" />

    <!-- Corner radius (opsional) -->
    <corners android:radius="558dp" />
</shape>
